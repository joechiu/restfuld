use JSON;
use Data::Dumper;
use lib '/srv/restful/lib';
use u;
my $t1 = _gettime;
$h = $params || { hello => 'world' };
my $t2 = _gettime;
$h->{realtime} = _timediff($t1,$t2);
logit "Results: "._to_json($h);
_to_json($h);
